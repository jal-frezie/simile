/*
launch.pl
---------
This is needed to make sure the system is loaded with the right syntax in place.
The command that actually starts it is in main. */

:- 	[syntax].
:-	use_module(main).
version_is(2.91).
